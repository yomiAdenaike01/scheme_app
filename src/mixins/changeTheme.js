export default {
  methods: {
    mutateTheme() {
      let theme = document.documentElement;
      // The colour picker must convert rgb to hsl
      // Update the theme setttings for that user
    }
  }
};
